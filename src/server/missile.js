class Missile {
  constructor(origin, destination, departureTime, eta) {
    this.origin = origin;
    this.destination = destination;
    this.departureTime = departureTime;
    this.eta = eta;
  }
}

export default Missile;
